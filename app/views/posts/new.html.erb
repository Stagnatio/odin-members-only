<%= render 'header' %>

<%= form_with model: Post.new do |form| %>
    <%= label(:title, "Title: ") %>
    <%= form.text_field :title %>
    <%= label(:body, "Post: ") %>
    <%= form.text_field :body%>
    <%= form.submit %>
<% end %>

<br>
<strong><%= link_to "Home", "/posts" %></strong>